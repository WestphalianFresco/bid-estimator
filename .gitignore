# Dependencies
node_modules/
.pnp
.pnp.js

# Secrets — must never enter the repo
.env
.env.*
!.env.example
*.pem
*.key

# Build artifacts
.next/
out/
build/
dist/
*.tsbuildinfo

# Tests and cache
coverage/
.vitest/
.cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Client data — estimate snapshots contain raw solicitation text and prices; keep them out of version control
snapshots/
*.snapshot.json

# Generated by `npm run refresh-prices` — store-specific and time-specific, regenerate rather than commit
material-prices.local.json

# Agent / editor notes and local tool config — personal, not part of the product
CLAUDE.md
AGENTS.md
.claude/
.mcp.json
*.local.json
